From 1882a310ef2d2bf8a43c42815d191f2669315349 Mon Sep 17 00:00:00 2001
From: vivekana <vivekana@xilinx.com>
Date: Tue, 4 Feb 2020 15:55:44 -0800
Subject: [PATCH 4/4] net:mcab: Remove unnecessary alignment check for TSO

---
 drivers/net/ethernet/cadence/macb_main.c | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/drivers/net/ethernet/cadence/macb_main.c b/drivers/net/ethernet/cadence/macb_main.c
index 8d84256..fd0d7f1 100644
--- a/drivers/net/ethernet/cadence/macb_main.c
+++ b/drivers/net/ethernet/cadence/macb_main.c
@@ -1767,7 +1767,7 @@ static netdev_features_t macb_features_check(struct sk_buff *skb,
 	/* Validate LSO compatibility */
 
 	/* there is only one buffer */
-	if (!skb_is_nonlinear(skb))
+	if (!skb_is_nonlinear(skb) || (ip_hdr(skb)->protocol != IPPROTO_UDP))
 		return features;
 
 	/* length of header */
-- 
2.7.4

